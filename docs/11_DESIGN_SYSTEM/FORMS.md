# Forms

**Estado: Pendiente de auditar.** Este documento existe como parte de la estructura completa del Design System (ver `README.md`) pero todavía no tiene contenido verificado contra el código.

Nota parcial (no una auditoría formal): `Field` (`frontend/components/crud-modal.tsx`) es el wrapper de etiqueta+input reutilizado por los formularios de los 8 módulos CRUD — ver `COMPONENTS.md` §Formularios. Una auditoría real debería confirmar convenciones de validación, mensajes de error, y si los formularios fuera del patrón CRUD (login, invitaciones, captura) siguen el mismo componente o uno propio.
