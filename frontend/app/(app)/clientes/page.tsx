import { Contact } from "lucide-react";
import { PendingModule } from "@/components/pending-module";

export default function ClientesPage() {
  return (
    <PendingModule title="Clientes" icon={Contact} description="Catálogo de clientes." />
  );
}
