import { Award } from "lucide-react";
import { PendingModule } from "@/components/pending-module";

export default function MarcasPage() {
  return (
    <PendingModule title="Marcas" icon={Award} description="Catálogo de marcas de producto." />
  );
}
